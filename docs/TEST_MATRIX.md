> 繁體中文：[docs/zh-Hant/TEST_MATRIX.md](zh-Hant/TEST_MATRIX.md)

# Test layers and current coverage

Layers follow plan §15. Tooling and product are separated: the fixture DSL and synthetic publisher
are test tools, never evidence about a real source app.

| Layer | Oracle | What exists | Run |
| --- | --- | --- | --- |
| L0 contracts & fixtures | Hand-written expectations | `core:testing` Fixtures DSL; every parser test is a synthetic fixture with an explicit expected batch | JVM |
| L1 JVM replay | Pure Kotlin parser / identity / reconcile / analytics | 79 tests in the JVM `core:*` modules (model 5, parser 13, identity 5, reconcile 22 incl. `ReconcilerIdAlignmentTest`, analytics 34), 45 in `parsers:apps`, 5 in `app` (reminders incl. `ReminderPolicy`); two 1,000-iteration property tests: distinct content must be accepted exactly once (seed 20260905), repeated id-less content must never duplicate and replays must never shrink the window (seed 20260906) | `./gradlew :core:model:test :core:parser:test :core:identity:test :core:reconcile:test :core:analytics:test :parsers:apps:test` |
| L2 Android publisher | Real notification callbacks through our own package | `SyntheticNotifications` (MessagingStyle, BigText); onboarding step 4 | Device, manual |
| L3 real source apps | Two consenting test accounts, recordings | **Not run** | — |
| L4 fault & performance | Process kill, Doze, first unlock, revoke, disk limits | **Not run** (commit fence exists in code; no injected-failure test yet) | — |
| L5 release artifact | Merged manifest, permission dump, reproducible build | `tools/check-permissions.sh` (CI); no SBOM / rebuild comparison yet | CI |
| Design system | Avatar monograms; locale-explicit time formatting | `MonogramTest` (6: Han, kana and hangul names give one glyph, Latin and right-to-left names two initials, blank gives `?`, an emoji is never cut in half), `TimeFormatTest` (2: Japanese and Korean dates and times render in the given locale, never the process default) | `./gradlew :core:designsystem:testDebugUnitTest` |
| String catalogues | Every locale carries the default catalogue's names, placeholders and plurals | `tools/check-strings.py` (en, zh-Hant, zh-Hans, ja, ko in `core:designsystem` and `platform:capture`); Android lint `MissingTranslation` is an error too | `python3 tools/check-strings.py` (CI: Assemble + permission gate) |
| Instrumented storage | Real SQLCipher + Room migrations | `VaultRoundTripTest` (journal → commit → search → suppression → reopen with persisted key; deleted conversation does not resurrect on replay; a repost of identical text that knows it was cut marks a row that said it was whole), `MigrationTest` (1→2, 2→3 and 3→4 against exported schemas; 3→4 asserts the two new nullable columns come back null on rows written before them, that a pending journal row keeps its payload and state and comes back unsettled — the truth about it, since 0.1.3 recorded such a loss nowhere — and that a gap and a settled journal row may then be inserted), `DemoDataTest` (debug demo seed → counts and projection → idempotent re-seed → clear leaves nothing; the seed is localised through `DemoLocalisation` for zh-Hans / ja / ko app languages), `DeletionGraphTest` (5: journal payload cleared on leaving PENDING; deleting the newest message rebuilds the projection; expired copies hidden before retention and projection rebuilt after it; removing a source with data leaves nothing behind; delete-everything verified and no cached cipher outlives the old key), `SearchPagingTest` (2: 250 false-positive candidates neither hide a true hit nor under-fill a page, pages resume by cursor without overlap; a deletion token suppresses a replay of the same post but not a later post with the same text), `MediaExportBoundTest` (1: `maxId` is 0 on an empty table and the bounded export page excludes a blob committed after the snapshot), `SourcePolicyTransactionTest` (13: a disable writes the flag and its gap together; setting a flag to the value it already holds writes nothing; a flag change whose gap write fails is not committed either; removing a source closes its gap in the same transaction; forgetting a source keeps the interval and drops the name; retention expires closed gaps and keeps the ones still happening; removing a source and its data takes the name off its gaps in one go and leaves another source's alone; disabling a source settles the losses its pending rows arrived with and discards those rows together, a settlement that fails leaves the row pending, the claim unspent and the source switched on, and a failure inside either remove branch leaves the source, its pending rows and its gap exactly where they were, including a `deleteData = false` callback that wrote something observable before it threw; and a removal that gets as far as the last of its seven graph deletions before a trigger aborts it puts every row back — conversation, messages, media rows, checkpoint, suppression, summary and diagnostics — and never touches the media file, which is deleted only from the list a committed transaction returns (the coordinator's own tests run against a mocked repository and can only show the hand-off, so what it is worth is decided here), `JournalLossTransactionTest` (21: accepting an event writes its loss with it; re-delivering the same event does not record it twice; an event whose loss cannot be written is not accepted either; a loss carried in from an older release is recorded by exactly one claim however many passes ask for it, a claim whose gap cannot be written is not spent, an event this release accepted is already settled and cannot be claimed again, a row that has left PENDING cannot be claimed at all, a settlement that keeps failing never costs the event its commit attempts, and the shape of the hole a row falls into when those attempts do run out (issue #28) — the same reason, for the guard, the transaction and the conditional update the coordinator's fakes were standing in for; the settle walk visits every pending row of a source exactly once and in order at six page sizes including one equal to the row count, with three rows sharing a millisecond so the tiebreak is load-bearing, and a whole page that will not decode still moves it on; its query plan is a seek into the composite index with no temporary B-tree; and a settlement that cannot be written leaves the row deferred with its payload, its claim and its commit attempts intact, out of the replay's page so the rows behind it are reached, back in it when a pass resumes, and still visited by the walk that runs before a source is discarded; a claim reports which of four things it found, and only two of them mean the gap is on disk; a settled row cannot be walked back to deferred; a page whose rows all fail to decode is empty and still points on; the query plan is explained from the statement the DAO itself runs, and asserts the cursor is part of the index range rather than a filter after it; and resuming one source's deferred rows leaves another's alone) — 51 tests | `./gradlew :platform:storage:connectedDebugAndroidTest` |
| Instrumented backup | Export → wipe → import on a real vault; the maintenance gate | `BackupRoundTripTest` (2: the backup holds only visible copies, reports media it could not read, restore rebuilds the projection and the media file decrypts under the current key; an export during an exclusive run is refused) | `./gradlew :platform:backup:connectedDebugAndroidTest` |
| Instrumented Compose semantics | What a screen reader actually reads for a message row | `MessageBubbleSemanticsTest` (5: in a group chat exactly one node in the merged tree carries both the sender and the body, the sender is read once and not twice — the negative control asserts they are two `Text`s in the unmerged tree, so the match is the merge working and not one composable; a shortened body carries the truncation chip and a complete one carries none, read from the string catalogue rather than hardcoded, because that label is the only thing on screen that says a stored body is not the whole body) | `ANDROID_SERIAL=<emulator> ./gradlew :feature:conversation:connectedDebugAndroidTest` |
| Instrumented crypto | Durable key write on a real filesystem; KEK creation race | `WrappedSecretFileTest` (data fsync → rename → `Os.fsync` on the directory; create-once, read-back, nested directory), `KeystoreWrapperTest` (three secrets created in parallel under a fresh alias, five rounds: one KEK, every secret readable from a new wrapper) | `./gradlew :platform:crypto:connectedDebugAndroidTest` |
| Crypto | RFC 5869 vectors, codec round trips | `HkdfTest`, `RecoveryKeyCodecTest` | JVM |
| Backup staging | Format and limit enforcement of the restore reader | `BackupStagerTest` (21 tests: manifest first, duplicate manifest, unsupported version, data after end, count mismatches, every size limit, unknown record) | `./gradlew :platform:backup:testDebugUnitTest` |
| Media pipeline | The failure mapping of a `content://` read, off a device | `MediaReadTest` (10: no stream from a provider that is gone is an expired link and never "too large"; over the cap is the only thing called too large; a revoked grant is a denial; a missing file is an expired link; a stream that dies mid-read is a failure, not a partial copy; an empty payload is an expired link; bytes that arrive are returned whole; a provider that never answers is abandoned instead of parking the caller; the caller is released while that read is still stuck, so a joiner — an exclusive maintenance run — is not blocked; a cancelled caller stays cancelled and does not become a failed copy) | `./gradlew :platform:media:testDebugUnitTest` |
| Capture coordinator | Commit fence, cold start and maintenance with mocked repositories | `CaptureCoordinatorTest` (63 tests: switching a source off opens a gap that names it and switching it back on closes it, pausing one source never closes another's gap, messages dropped before parsing are a gap even though the ingest that followed committed the surviving messages, lines an InboxStyle notification could not hold are a gap rather than a shortened body, and its negative control — a message whose text was merely shortened is not a gap; that loss is written in the transaction that accepts the event, so an event the journal already holds does not record it a second time and replaying a pending row does not record it again; a row a release up to 0.1.3 left pending has its dropped lines settled once however often it is replayed, a flag of that vintage that can only mean whole messages were dropped is settled too, one that could equally be a single shortened message is left alone, and rows discarded with their source are settled while their payload can still be read; a loss neither the journal nor its fallback gap could record is kept and written by the next thing that proves the vault writable — a policy load, or simply the next accepted event, with no policy change at all; setting a source flag to the value it already has is not a second gap, removing a source closes the gap it left open and — only when the data goes too — forgets its name, and a gap an earlier version left open against its own policy is closed when the policy loads; pause discards queued events, resume rotates generation and session, non-source packages dropped after the source list loads, cancellation propagates; a source disabled while an event waits for the lock is never journaled, a pause between acceptance and commit leaves the event pending, replay is held while paused and runs on resume, replay discards a row whose source was disabled, a maintenance run drops the queue and records an exact gap, every maintenance run records its own gap; a notification is held unread before the policy is known and only sources are snapshotted, held notifications are dropped with a bounded gap when the vault does not open; bitmaps in flight still count against the queue bound; an overflowed hold buffer records the drop and keeps only sources; a journal insert that throws is recorded as a gap; a cold-start loss and a pipeline lock-out the locked vault could not record are written as bounded gaps once it opens; a loss whose settle failed is kept and written on the next policy load, a gap row that lands after the policy loaded is closed at once, a notification held across a disconnect gets a gap of its own, but none when its source is paused or the resync captured it again; a gap row landing between the settle and the flag flip is closed by the policy load; an overflow gap whose write failed is kept and written on the next policy load; a stale copy with the same key but an older post time is a loss of its own; a disconnect landing during the release gives the later notifications a gap instead of letting them suppress themselves; an app the user never enabled has only its package name read; a settlement the vault refuses defers its row instead of holding the page, so two hundred of them no longer starve the two hundred and first — nor a second source's row in the same pass — and no commit attempt is charged for any of it; the deferred rows are retried by the next acceptance that wrote a gap, with an acceptance that wrote none as the negative control; a deferred row is still settled before a disable or a remove discards it; and pending rows removed with their source are settled while their payload can still be read, which `removeSource` had no test for at all; a claim that took nothing says which of four things it found, so a row another pass deferred is never committed on the strength of a `false` that used to mean the gap was already written; replay passes are coalesced, so two triggers arriving together run one pass and no two passes hold overlapping pages; a failing prefix longer than a whole pass no longer keeps the rows behind it from ever being read, at a page size and round limit set for the test; a replay that cannot even defer a row does not claim it made progress; and the pending rows of a source with more than one page are all settled before it is disabled) | `./gradlew :platform:capture:testDebugUnitTest` |
| Storage logic (JVM) | Maintenance gate ordering, reset failure branches, suppression rule | `VaultMaintenanceTest` (5: work runs and returns; refused while exclusive is active; exclusive cancels and joins work in flight; a listener sees start/end exactly once even for an instant run; exclusive runs and pipeline-lock holders are serialised), `VaultRepositoryTest` (3: database / media failure names the step, keeps keys and reopens the vault; happy path), `SuppressionRuleTest` (4) | `./gradlew :platform:storage:testDebugUnitTest` |
| Search / conversation ViewModels | Locked and opening vault with mocked repositories | `SearchViewModelTest` (5: locked is reported as locked and the query is not run; a query typed while opening runs once ready and again after a retry; a full page keeps its cursor so the header can never call it a total; a page that verifies nothing keeps its cursor, because an empty page means the candidate scan budget ran out and not that the index is finished; a page still in flight when the query changes is discarded rather than appended to someone else's results), `ConversationViewModelTest` (1: opening keeps loading, locked is shown as locked, ready shows the rows) | `./gradlew :feature:search:testDebugUnitTest :feature:conversation:testDebugUnitTest` |
| Onboarding verdict | What the capture test may call a success, with mocked repositories | `OnboardingViewModelTest` (5: a vault that already holds messages does not make the test pass; one of three captured is not a success; three of three is a success and is never called a failure; a capture that never arrives is reported as a failure instead of spinning for ever; a copy that arrives late is a success, not a timed-out failure) | `./gradlew :feature:onboarding:testDebugUnitTest` |
| Analytics ViewModel | State rules of the activity page with mocked repositories | `AnalyticsViewModelTest` (8 tests: first report computed off the collector's thread, period switch shows a clean placeholder without the previous period's capped label, vault change recomputes without a loading state, locked vault shown as locked and recovers once unlocked, a vault that locks while the page is open recovers without a count change, opening vault keeps loading then computes, failing count query does not leave the page loading, a failing query marks the report as degraded) | JVM |

## Scenario ids referenced by the plan (subset implemented as tests)

| Plan example (§7.2) | Test |
| --- | --- |
| `[A] → [A,B] → [A,B,C]` ⇒ A B C | `ReconcilerTest` "yields exactly A B C" |
| only `[A,B,C]` ⇒ three messages | "stores all three, not just C" |
| `[A,B,C] → [B,C,D]` ⇒ A B C D | "keeps A B C D" |
| `[好(id=1), 好(id=2)]` ⇒ two | "are two messages" |
| `[好(?)] → [好(?)]` ⇒ ambiguous | "is an ambiguous observation" |
| `[A,B,C]` then old `[A]` ⇒ keep B C | "does not delete B C" |
| closed `[A,B,C]` → `[C]` (new post) → `[B,C,D]` ⇒ B C known, only D new | `ReconcilerAmbiguousKeepTest` |

## Demo mode (debug builds only)

`DemoDataRepository` (`platform:storage`) fills the vault with obviously synthetic content so the
app can be demonstrated, walked through and screenshotted without exposing a real notification. It
writes three fictional sources under the `demo.quietinbox.` package prefix, eight invented
conversations (bilingual titles, one pinned, one archived, all three identity-confidence levels) and
roughly 130 messages spread over the last 30 days with an evening-weighted hour distribution, plus
one closed capture session, two gap intervals, three diagnostic events and two summary-only
observations. Deliberately included: an `AMBIGUOUS_REPEAT` pair with its observation link, a revised
message with its previous body, a `PLACEHOLDER_ONLY` image, a preview-restricted body, self
messages, long text, emoji and URLs — so every honesty label in the UI has a row behind it. Rows are
shaped exactly as `IngestRepository.commit` shapes them (same fingerprint, sort-key rule and search
tokenisation), so the demo exercises the real read paths.

Nothing about it is real: no name, group, brand or app in the seeded data refers to anything that
exists, and no source notification is ever read.

- Trigger from adb (debug APK only; the receiver lives in `app/src/debug`):
  ```bash
  adb shell am broadcast -a dev.quietinbox.debug.DEMO --es op seed \
      -n dev.quietinbox.app.debug/dev.quietinbox.debug.DemoReceiver
  adb shell am broadcast -a dev.quietinbox.debug.DEMO --es op clear \
      -n dev.quietinbox.app.debug/dev.quietinbox.debug.DemoReceiver
  ```
- Trigger from the app: Settings → Developer → "Fill with demo data" / "Remove demo data". The
  section is rendered only when the injected `BuildInfo.debug` is true, so it does not exist in a
  release build.
- `seed()` is idempotent (it clears first) and `clear()` deletes strictly by the demo tags — the
  `demo.quietinbox.` package prefix and the `demo-` capture generation — so captured copies are
  never touched. No schema change: the seeder adds queries, not tables or columns.
- Screenshot harness: `tools/demo-screenshots.sh <adb-serial> <en-US|zh-TW|zh-CN|ja-JP|ko-KR> <out-dir>` installs the
  debug APK, wipes app data, grants the listener and `POST_NOTIFICATIONS`, walks onboarding by
  matching button text in both languages, seeds the demo vault and captures
  `1_inbox.png … 7_inbox_dark.png`. Use an emulator: on a real phone the listener would copy the
  owner's own notifications into the debug vault. On Android 13+ the keyboard follows the app language, so the tool disables the default input method before it launches the app, restores it afterwards, types the query one key at a time once the input method has settled, dismisses it with ENTER (the single-line field's Done action; the search is live, nothing is submitted) and refuses to shoot the search screen if the field does not show the query or an input method is still showing; every shot must also be at least 80 KB (calibrated on the 1080×2400 `QuietInbox_Phone`), the conversation shot waits (on the narrow layout) until the pinned title is on screen and the bottom bar is gone, and fails the run otherwise, the app language is set and confirmed before anything starts the process (and the process is stopped again before the launch), the seed broadcast names the demo's language (`--es lang`), and a non-English run refuses the inbox, conversation, activity and capture shots if an English AM/PM time or month appears in the app's own nodes — the sign that the process locale lagged behind the app language (the detector assumes an English device language, and the English run proves it still bites on its inbox clock).
  Several guards decide whether a file is written at all. **The app must be the app on screen**: before
  every capture the UI dump must contain a node belonging to `dev.quietinbox.app.debug`. **A navigation
  tap must take**: the tap is re-sent up to five times and the run fails unless the tapped item becomes
  the selected one (a node of the app, marked `selected`, inside the navigation strip and covering the
  label) — proving the app is on screen does not prove the right page is. **A dark shot must be dark**:
  failing to switch the device into night mode fails the run. The UI dump itself retries, since every
  capture now depends on one. The first two are what the tablet set was missing. Without those two the first tablet set captured
  the launcher and the system settings, and the size floor passed them (a wallpaper compresses to 3.3 MB).
  **Both layouts are supported, on two separate breakpoints**: the harness reads the window width in dp
  and taps the navigation rail at ≥ 600dp, the bottom bar below it; separately, only at ≥ 840dp does the
  conversation open beside the inbox, and there it counts as ready when the pinned title is on screen
  twice (the list row and the detail header) and no BACK is sent, because the rail never went away.
  Between 600dp and 839dp the rail is up but the conversation is alone — the band the app itself used to
  get wrong (FT-02), which is why the harness keys the two decisions separately. Tablet shots go to `docs/screenshots/tablet/<locale>/`
  and `fastlane/metadata/android/<locale>/images/tenInchScreenshots/` (en-US and zh-TW so far, captured on
  `Foldable_Test`, 2076×2152).
- Coverage: `DemoDataTest` (instrumented, `platform:storage`) seeds, asserts the row counts and the
  conversation projection every screen reads, checks that seeding twice does not duplicate, then
  clears and asserts nothing demo-tagged remains. It runs on a device
  (`./gradlew :platform:storage:connectedDebugAndroidTest`) because SQLCipher's native library
  cannot load on the JVM.

## Not covered yet

- The `feature/*` ViewModels with JVM tests are Analytics (8), Search (5), Onboarding (5) and
  Conversation (1); the rest have none. The locked-vault state and the "report may be incomplete"
  label of the activity page are covered by `AnalyticsViewModelTest` but have not been exercised on
  a device. The test harness does not cancel `viewModelScope` (each test owns its ViewModel).

## Quantitative targets (plan §15) — status

All numeric targets (callback p95 < 10 ms, commit p95 < 500 ms, search p95 < 300 ms on 100k rows,
72 h soak) are **unmeasured**. No benchmark module has been run; the values in the plan remain
planning thresholds, not results.
